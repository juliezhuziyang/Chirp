# Railway sets PORT automatically. Do not hardcode in production.
PORT=8000

# Comma-separated browser origins allowed to call this API (your frontend URL).
# Example: https://chirp.example.com,http://localhost:5173
CHIRP_ML_CORS=*
